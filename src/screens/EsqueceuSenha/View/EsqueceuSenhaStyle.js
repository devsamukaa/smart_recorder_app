import { StyleSheet } from 'react-native';

export default StyleSheet.create({

    container_logo: {
        marginTop: 16,
        height: 100,
        justifyContent: 'center',
        alignItems: 'center',
    },

});