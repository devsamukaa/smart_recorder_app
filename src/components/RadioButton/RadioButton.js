{/* <View style={[styles.align_center]}>
    <RadioForm
    formHorizontal={true}
    animation={true}>
    {
        radio_props.map((obj, i) => (
        <RadioButton labelHorizontal={true} key={i} >
            <RadioButtonInput
            obj={obj}
            index={i}
            isSelected={tipoPessoa === i}
            onPress={(value) => {setTipoPessoa(value)}}
            borderWidth={1}
            buttonInnerColor={'#fff'}
            buttonOuterColor={tipoPessoa === i ? '#fff' : '#fff'}
            buttonSize={12}
            buttonOuterSize={22}
            buttonStyle={{}}
            buttonWrapStyle={{marginLeft: 10}}
            />
            <RadioButtonLabel
            obj={obj}
            index={i}
            labelHorizontal={true}
            onPress={(value) => {setTipoPessoa(value)}}
            labelStyle={{fontSize: 20, color: '#fff'}}
            labelWrapStyle={{}}
            />
        </RadioButton>
        ))
    }  
    </RadioForm>
</View> */}